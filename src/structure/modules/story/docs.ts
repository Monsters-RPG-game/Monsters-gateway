/**
 * @openapi
 * components:
 *   schemas:
 *     INpcStoryEntity:
 *       type: object
 *       properties:
 *         _id:
 *           type: string
 *         login:
 *           type: string
 *         verified:
 *           type: string
 *       required:
 *         - _id
 *         - login
 *         - verified
 */
  // _id: string;
  // name: string;
  // npcId: string;
  // lines: ILine[];

/**
 * @openapi
 * components:
 *   schemas:
 *     IUserEntity:
 *       type: object
 *       properties:
 *         _id:
 *           type: string
 *         login:
 *           type: string
 *         verified:
 *           type: string
 *       required:
 *         - _id
 *         - login
 *         - verified
 */
